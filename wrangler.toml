name = "cypher"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# Banco D1 (já criado na conta). O binding "DB" é o que a API usa (env.DB).
[[d1_databases]]
binding = "DB"
database_name = "cypher-db"
database_id = "02d78aae-e72b-4f6d-a96f-7de0bb84a718"

# Segredos NÃO ficam aqui. Configure-os como secrets/variáveis de ambiente:
#   AUTH_SECRET  -> chave para assinar os tokens de login (obrigatório)
#   INVITE_CODE  -> código exigido para criar acesso (recomendado)
# Ver README.md.
